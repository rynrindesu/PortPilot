# Point the console at the running services. Leave unset to use the bundled
# operating-day snapshot (no database, no credentials required).
VITE_RESCHEDULING_API=http://127.0.0.1:8000
VITE_PORT_OPS_API=http://127.0.0.1:8001
