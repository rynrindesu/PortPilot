# Sample documents

Drop maritime PDFs here to run them through the real extraction pipeline
(text-layer parse → Textract OCR fallback → classify → extract → validate).

Useful mix:
  - certificate of registry
  - arrival general declaration
  - crew list
  - cargo declaration
  - at least one scanned/photographed PDF, to exercise the OCR path

Not committed: see .gitignore.
